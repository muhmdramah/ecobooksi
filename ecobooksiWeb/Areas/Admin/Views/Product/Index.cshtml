@model List<Product>
@{
    ViewData["Title"] = "Products";
}

<div class="card shadow border-0 my-4">
    <div class="card bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Products</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-action="Upsert"
                   asp-controller="Product"
                   class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Create A New Product
                </a>
            </div>
        </div>
        <table id="tblData" class="table table-bordered table-striped" width="100%">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>ISBN</th>
                    <th>Price</th>
                    <th>Author</th>
                    <th>List Price</th>
                    <th></th>
                </tr>
            </thead>

            @* <tbody>
                
                @foreach (var product in Model.OrderBy(order => order.ListPrice))
                {
                    <tr>
                        <td>@product.Title</td>
                        <td>@Html.Raw(product.Description)</td>
                        <td>@product.ISBN</td>
                        <td>@product.Author</td>
                        <td>@product.ListPrice</td>
                        <td>@product.Price</td>
                        <td>@product.PriceFifty</td>
                        <td>@product.PriceHundred</td>
                        <td>@product.Category.CategoryName</td>
                        <td>
                            <div class="w-75 btn btn-group" role="group">
                                <a asp-action="Details" asp-controller="Product"
                                   asp-route-productId="@product.ProductId" class="btn btn-primary mx-2">
                                    <i class="bi bi-info-circle"></i> Details
                                </a>

                                <a asp-action="Upsert" asp-controller="Product"
                                   asp-route-productId="@product.ProductId" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-action="Delete" asp-controller="Product"
                                   asp-route-productId="@product.ProductId" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash3"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
  

            </tbody> *@

        </table>
    </div>
</div>

@section Scripts
{
    <script src="~/js/product.js"></script>
}